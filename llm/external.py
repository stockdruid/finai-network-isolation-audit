"""외부 LLM (Mock) — 개발자 A 구현.

VPC B 의 Mock LLM Server (api.openai-mock.example.com) HTTP egress.
호출 자체가 EV-001 위반 시나리오 — target_url을 chatbot_logs에 기록.
"""

# TODO(개발자 A): call_mock_llm(prompt) -> response, target_url
